use configuration_parameters::ConfigurationParameters;
use slog::Logger;

pub fn parameter_calls(
    config_params: &ConfigurationParameters,
    logger: &Logger,
    diag_logger: &Logger,
) {
    println!("hello world");
}
